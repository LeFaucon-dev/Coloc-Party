<div class="nav">
	{foreach $menu as $key => $value}
	<a href="{$value}">{$key}</a>
	{/foreach}
</div>

<!--<div class="nav">
    <ul id="navigation">
        {foreach $menu as $key => $value}
        <li class="button"><a href="{$value}">{$key}</a></li>
        {/foreach}
    </ul>
</div>-->